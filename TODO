Add feature so that on loss of focus each square automatically swaps back to label view and retains the last inputted value

Perform value input checks

Why not move the signal handlers from MainWindow to SudokuSquare?


I need to add an event handler for when a Square's value changes

Make 0, a blank when read from a saved file
